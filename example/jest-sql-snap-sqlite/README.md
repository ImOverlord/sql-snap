# Jest-SQL-Snap-Sqlite

Example project using Jest, SQL Snap, and Sqlite.

## Getting Started

```bash
$> npm ci
```

IF you get an error, installing sql-snap, it might be because it can resolve the relative link. You try and directly install sql-snap.
